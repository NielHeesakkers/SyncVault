## [2.5.34] — 2026-04-02
- Robust JSON decoder for FileProvider: handles Int/String type mismatches, unblocks on-demand uploads

## [2.5.33] — 2026-04-02
- Block root-level file creation for non-admins, clean root, auto-expand user folder in browser

## [2.5.32] — 2026-04-02
- FileProvider: always use block upload, fix 500 errors on multipart upload

## [2.5.31] — 2026-04-01
- Fix FileProvider JSON decode: optional fields, removedLocally bool, menu bar icon shows on-demand activity

## [2.5.30] — 2026-04-01
- FileProvider block upload: no temp files, direct to server, dedup, real progress per block

## [2.5.29] — 2026-04-01
- Fix on-demand progress: show real network speed, not local disk speed; show transfer graph during on-demand

## [2.5.28] — 2026-04-01
- On-demand progress: 0.5s polling, live speed, bytes transferred, speed history graph

## [2.5.27] — 2026-04-01
- Show FileProvider on-demand upload/download progress in menu bar

## [2.5.26] — 2026-04-01
- Fix on-demand large file upload: streaming from disk instead of loading into memory

## [2.5.25] — 2026-04-01
- Fix password reset flow: token invalidation, base_url in reset link, sidebar hidden on public pages

## [2.5.24] — 2026-04-01
- Beautiful HTML email templates with branded layout

## [2.5.23] — 2026-04-01
- Fix SMTP TLS for port 465, forgot password sidebar fix, integration tests

## [2.5.22] — 2026-04-01
- Memory and CPU optimizations: streaming storage reads, request body limits, timer cleanup, reduced timeouts

## [2.5.21] — 2026-03-31
- File locking (30min auto-expire), SSE push notifications, all features verified

## [2.5.20] — 2026-03-31
- Fix on-demand: cloud-only file states, domain re-registration, eviction support

## [2.5.19] — 2026-03-31
- On-demand: remove local folder picker, auto CloudStorage path, monitored paths sync

## [2.5.18] — 2026-03-31
- Finder Sync Extension: overlay icons and context menu, release script updates

## [2.5.17] — 2026-03-31
- Conflict detection, file search, preview modal, activity feed, Finder Sync Extension prep

## [2.5.16] — 2026-03-30
- Fix FileProvider: add GET /api/files/{id} endpoint, fix getFile 404 error

## [2.5.15] — 2026-03-30
- On-demand sync: streaming download, auto re-auth, eviction support (pin/free)

## [2.5.14] — 2026-03-30
- Settings window focus fix, remove LAN/external URL fields, auto-test server, auto re-auth after sleep

## [2.5.13] — 2026-03-30
- Atomic rename sync, change journal baseline fix, multi-client two-way sync

## [2.5.12] — 2026-03-30
- Change journal for correct rename/delete sync, two-way directory sync, periodic remote change detection

## [2.5.11] — 2026-03-30
- Sync wizard with remote folder browser, auto-detect LAN/extern, bandwidth limiter, selective sync, progress tracking fix, speed graph, theme toggle

## [2.5.10] — 2026-03-29
- Streaming downloads, Docker Desktop migration, upload/download icons fix

## [2.5.9] — 2026-03-29
- Add sync task wizard with remote folder browser, security hardening, and sync fixes

## [2.5.8] — 2026-03-29
- Fix auto-updater sandbox permission — use Application Support instead of /tmp

## [2.5.7] — 2026-03-29
- Fix folder creation race condition — idempotent find-or-create for directories

## [2.5.6] — 2026-03-29
- Fix damaged app — proper signing with ditto and entitlements

## [2.5.5] — 2026-03-28

macOS App:
- Fixed "damaged app" error: proper ditto-based zip for code signing preservation
- Zero-copy streaming upload: no temp files, 4MB memory buffer only

Server:
- Streaming Put(): 4MB block processing instead of loading full file in memory
- Fixes OOM on chunked upload assembly for files >1GB

## [2.5.3] — 2026-03-27

macOS App:
- Chunked upload with 10 parallel connections (was 1 serial)
- Automatic retry on connection lost (-1005) for all HTTP calls
- Upload session caching: resume after failure instead of re-uploading
- Complete call retries 5x with increasing delay
- Progress bar and speed now update per chunk during upload

Server:
- Streaming storage: Put() now streams in 4MB blocks instead of loading entire file into memory
- Fixes OOM crash when assembling large files (>1GB) after chunked upload

## [2.5.0] — 2026-03-26

Backend/Web:
- Hash check endpoint: clients skip uploading files server already has
- File tree endpoint: flat recursive listing for faster sync comparison
- Light/dark theme toggle with CSS custom properties
- All pages refactored to use theme variables

macOS App:
- Dynamic parallel uploads (2-16 based on file size)
- Chunked upload for files >50MB with parallel chunks and resume
- Speed graph sparkline in menu bar (last 10 min)
- Progress bar fix during active uploads
- Auto-reconnect with saved credentials
- No more phantom downloads (hash-based dedup)

## [2.4.0] — 2026-03-25

Backend/Web:
- Dark theme redesign: professional dark UI with colored file type icons
- Dashboard homepage with storage widget and recent activity
- Loading skeletons and empty states throughout
- Chunked resumable uploads: files >64MB uploaded in 64MB chunks, resume on failure
- Delta sync: only upload changed blocks of modified files (256KB block signatures)
- Server-side hashing: client no longer needs to hash before upload
- Storage cleanup: calendar date picker, preview what gets deleted, type DELETE to confirm
- Token invalidation on password change: user logged out everywhere
- Cleanup tab in Settings with visual data calendar

macOS App:
- Glassmorphism menu bar popover with vibrancy
- Pulsing status dots, colored mode badges, card-style task list
- System Settings-style grouped forms
- 8 parallel uploads (was 4)
- Chunked upload for large files (>64MB)
- Delta sync: only changed blocks uploaded
- No upload timeout (infinite)
- Skip client-side hashing (server does it)

## [2.3.0] — 2026-03-25

Backend/Web:
- Files now have 'removed_locally' field — locally deleted files stay visible on server
- Sync state sync endpoints: known state stored per user/device on server
- 'Removed locally' label shown next to files in web UI file browser
- New endpoints: PUT /api/files/{id}/removed-locally, GET/PUT/DELETE /api/sync-state

macOS App:
- Synology Drive-style sync engine: FSEvents triggers immediate sync (no 30s wait)
- 4 parallel uploads with AsyncSemaphore concurrency control
- No cycle limit — all pending files processed in one pass
- Small files uploaded first, large files last
- Upload speed shown in menu bar (green ↑ / purple ↓)
- Known state synced to server after each cycle (restore-to-new-Mac support)
- Files deleted locally are marked on server instead of deleted

## [2.2.1] — 2026-03-25

macOS App:
- Recursive directory sync: local folder structure is mirrored identically on server
- FSEvents file watcher: only changed files are hashed and synced (not full scan every 30s)
- Smart hashing: skips files whose modification date hasn't changed since last sync
- Incremental sync: after initial full scan, only processes FSEvents-reported changes
- Subdirectories automatically created on server during upload
- Max 10 actions per sync cycle (up from 5)

## [2.2.0] — 2026-03-25

macOS App:
- Fix crash when syncing folders with duplicate filenames in subdirectories
- Dictionary(uniqueKeysWithValues:) replaced with uniquingKeysWith to handle duplicates safely

## [2.1.9] — 2026-03-25

macOS App:
- True streamed upload: file never loaded in memory, streamed from disk via URLSession
- Hash computed in 256MB chunks without memory allocation
- Fixes crash on GB-sized files

## [2.1.8] — 2026-03-25

macOS App:
- Streamed upload: files read in 256MB chunks (no more crashes on large files)
- Menu bar icon: blue/green blink while syncing, green when up to date

## [2.1.7] — 2026-03-25

Backend/Web:
- Fix delete user: clean up connection_tokens, password_resets, retention_policies

macOS App:
- Fix auto-update: use /tmp/ for update script (works outside sandbox)
- Identical update mechanism as AutoConvert (proven reliable)

## [2.1.6] — 2026-03-25

Backend/Web:
- Server auto-deletes old sync task with same name on recreate (permanent 409 fix)

macOS App:
- Upload limit: max 5 files per sync cycle (prevents app freeze)
- Delete sync task removes from server
- Server task ID tracked per sync task

## [2.1.5] — 2026-03-25

Backend/Web:
- Auto-login endpoint for macOS app browser handoff

macOS App:
- Delete sync task now also removes it from server (fixes 409 on recreate)
- Server task ID tracked per sync task
- Open Files on Server with auto-login
- Test Server button always visible
- Password field shows dots when saved
- Changelog and Info tabs in Settings

## [2.1.4] — 2026-03-25

Backend/Web:
- Auto-login: macOS app opens web UI without manual login
- 409 duplicate folder fix improved (parent_id matching)

macOS App:
- "Open Files on Server" with auto-login via JWT token
- "Test Server" button always visible in Connection tab
- Password field shows dots when saved in Keychain
- Changelog tab loads release notes from server
- Info tab with contact email and GitHub link

## [2.1.3] — 2026-03-25

Backend/Web:
- SMTP: SSL/TLS support for port 465 (Purelymail, Gmail)
- Backup: configurable interval (hours/days/weeks)
- Trash: admin sees all users' trashed files
- Files: duplicate folder names auto-renamed to _DELETED_[date]
- Files: folder delete reloads current view correctly
- Settings: test buttons show green/red feedback for 5 seconds
- Users: combined edit modal with Details/Token/Delete tabs
- Users: type DELETE to confirm user deletion
- Info: added contact email

macOS App:
- Sync engine: re-throws 401 for auto re-authentication
- Saved username to Keychain for re-auth
- On-demand sync mode added to task picker

## [2.1.2] — 2026-03-23
- Menu bar redesign, settings sidebar, sync queue, on-demand mode, Check for Updates fix

## [2.1] — 2026-03-23
- Sidebar shows server version dynamically
- Changelog tab shows correct version number
- Removed Changelog link from sidebar (now in Settings → Changelog)
- Merged duplicate v1.9 entries

## [2.0] — 2026-03-23
- One-time encrypted connection tokens (.syncvault files)
- Token refresh with PIN re-emailed to user
- JWT token TTL extended to 24h with auto re-auth
- Sync engine: fixed upload failures (401 auth errors)
- Combined edit user + password reset dialog
- Test email fix (no more hanging)
- Password reset sends warning email
- Share link delete in backend
- Retention settings removed from macOS app
- Admin password reset via Docker env var

## [1.9] — 2026-03-23
- Password recovery and forgot password UI
- Smart admin password reset via Docker env var
- Security-scoped bookmarks for sync folders
- Sync progress UI with file names and speed

## [1.8] — 2026-03-22
- Admin password reset CLI, security-scoped bookmarks, sync progress UI, os.Logger debugging

## [1.7] — 2026-03-22
- Fix Keychain persistence across updates, add EdDSA signing for Sparkle

## [1.6] — 2026-03-22
- Fix sync engine, team folder navigation, admin file visibility

## [1.5] — 2026-03-22
- Version Explorer with interactive SVG timeline
- Browse files at any point in time, click dots to navigate history
- Hover effect on timeline dots
- Select files/folders to highlight their versions on the timeline (orange)
- Download folders as ZIP at any point in time
- Restore entire folders to a previous state
- Retention policy per sync task (hourly, daily, weekly, monthly, yearly)
- Share files and folders with password, expiry, download limit, email notification
- Team folders visible in Files with blue folder icons
- Admin sees all user folders and team folders at root level
- Folder sizes calculated recursively and shown in file browser
- Multi-select with shift-click in Trash
- Empty Trash button changes to Remove (N) when items are selected
- User delete with transfer option: move files to another user
- Team delete with transfer option: move folder to a user
- Team edit: rename and set storage quota (MB/GB/TB)
- Storage quota input with MB/GB/TB unit selector for users and teams
- Storage page: separate User and Team sections
- Settings redesigned with tabs: General, Notifications, Backup, Changelog, Info
- Settings backup: create, download, restore, upload, auto-daily toggle
- Notifications system with bell icon and unread badge
- Team invite notifications with Accept/Decline
- Browser back/forward navigation works within file browser
- Base URL setting for share links
- Trash retention days setting
- Send test email with editable recipient
- Open Source info page with credits
- Changelog moved to Settings tab

## [1.1] — 2026-03-21
- Auto-create user root folder on user creation
- Sync task management: create, list, delete tasks via /api/tasks
- Task types: sync (two-way), backup (upload only), ondemand (one per user)
- Auto-create task subfolders (Sync-X, Backup-X, OnDemand)
- File Provider Extension for on-demand sync in macOS Finder
- Changelog page in web UI
- Version number shown in sidebar and /api/health
- Faster Docker builds (arm64 only)
- SMTP settings configurable via admin web UI
- Fix: folder creation, file upload, user creation, storage page
- Fix: SMTP settings save with numeric port

## [1.0] — 2026-03-21
- Initial release
- File sync and backup platform
- Content-addressable storage with deduplication
- File versioning with delta patches (up to 32 versions)
- JWT authentication with role-based access control
- Web interface: file browser, version history, sharing, admin panel
- macOS menu bar sync client
- SMTP email notifications
- Docker single-container deployment
- Team folders with per-user permissions
- Share links with password, expiration, download limits
- Activity log and storage insights
- Retention policies (daily/weekly/monthly/yearly)
- Sparkle auto-update for macOS app
